import './App.css';

function App() {
    return (

        <
        div className = 'border-gray-400 border rounded-2xl p-2 m-2 flex justify-around items-center' >
        <
        h1 className = "text-3xl font-bold underline" > Hello tailwind css < /h1> <
        p className = "m-0 text-gray-400 " > tailwindcss < /p> <
        button className = "bg-gray-300 border-2 p-2 rounded-md hover:text-red-500 " > send < /button>  <
        /div>
    );
}

export default App;